//tuple :kind of special aary given by TS 

export { }

let tuple_user: [String, number, boolean];

tuple_user = ["anil", 32, false]

let rgb: [number, number, number];
rgb = [22, 33, 44]

type bus_info = {
    name: string,
    seats: number
}

//const tuple_bus: bus_info = [{ name: "Volvo", seats: 24 }];
//tuple_bus[1].name

type car = [boolean, number]

const carvalue_sale: car = [false, 888888]


function thupas(a: number, b: string): string {

    return "";
}

let chamanprash = (a: number, b: number): number => {

    return a + b;
}


